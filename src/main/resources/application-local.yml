server:
  port: 8080

spring:
  config:
    activate:
      on-profile: local

  data:
    redis:
      host: localhost
      port: 6379

  datasource:
    url: jdbc:mariadb://34.64.218.29:3306/meetty_db
    username: ${PROD_USERNAME}
    password: ${PROD_PASSWORD}
    driver-class-name: org.mariadb.jdbc.Driver

  jwt:
    secure-cookie: false

